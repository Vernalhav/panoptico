// Carrega lista de todos os tópicos do BD
// Filtra essa lista (na RAM) fazendo um array.filter()
//    com a condição de filtro sendo o match em algum
//    dos patterns
// retornar lista
// getTopicsByRegexList(regexList: string[]): string[]